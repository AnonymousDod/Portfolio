[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["cd server && npm install"]

[phases.build]
cmds = ["echo 'Skipping React build - backend only'"]

[start]
cmd = "cd server && node index.js"

